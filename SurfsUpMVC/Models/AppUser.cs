using Microsoft.AspNetCore.Identity;

namespace SurfsUp.Models
{
    // Add AppUser as userclass, derived from IdentityUser
    // Gives us oppertunity to add properties and soforth
    
    public class AppUser : IdentityUser
    {
    }
}
